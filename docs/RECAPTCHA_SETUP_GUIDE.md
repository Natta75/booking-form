# 🛡️ Подробная инструкция по настройке Google reCAPTCHA v3

## Что вам нужно получить:
1. **Site Key** (публичный ключ) - используется на фронтенде
2. **Secret Key** (секретный ключ) - используется на бэкенде

---

## 📝 Пошаговая инструкция

### Шаг 1: Переход в Google reCAPTCHA Admin Console

1. Откройте браузер и перейдите по ссылке:
   ```
   https://www.google.com/recaptcha/admin/create
   ```

2. **Войдите в Google аккаунт**, если еще не вошли

---

### Шаг 2: Создание нового сайта reCAPTCHA

Вы увидите форму создания нового сайта. Заполните следующие поля:

#### 2.1 Label (Название)
- **Что вводить**: Любое понятное вам название
- **Пример**: `Booking Form` или `Форма бронирования`
- Это название только для вас, пользователи его не увидят

#### 2.2 reCAPTCHA type (Тип reCAPTCHA)
**⚠️ ВАЖНО:** Выберите **"reCAPTCHA v3"**

Вы увидите несколько вариантов:
- reCAPTCHA v3 ✅ **← Выберите этот!**
- reCAPTCHA v2 ("I'm not a robot" Checkbox)
- reCAPTCHA v2 (Invisible reCAPTCHA)

**Почему v3?**
- Невидимая для пользователя (не нужно кликать "Я не робот")
- Работает в фоне и оценивает поведение пользователя
- Ваш код уже настроен на работу именно с v3

#### 2.3 Domains (Домены)
**Что вводить**: Список доменов, где будет работать форма

**Для разработки и тестирования добавьте:**
```
localhost
127.0.0.1
```

**Для production (если у вас уже есть домен):**
```
yourdomain.ru
www.yourdomain.ru
```

**Как добавлять:**
- Вводите по одному домену
- Нажмите Enter или кнопку "+" после каждого
- НЕ добавляйте `http://` или `https://` - только домен!
- НЕ добавляйте порт (`:3000`) - только домен!

**Пример правильного заполнения для разработки:**
```
localhost
```

**Пример правильного заполнения для production:**
```
mysite.ru
www.mysite.ru
```

#### 2.4 Owners (Владельцы)
- Автоматически добавится ваш email
- Можно добавить дополнительных владельцев, если нужно
- **Для начала оставьте как есть**

#### 2.5 Accept the reCAPTCHA Terms of Service
- **Поставьте галочку** ✅ "Accept the reCAPTCHA Terms of Service"
- Прочитайте условия, если нужно

---

### Шаг 3: Создание и получение ключей

1. Нажмите кнопку **"Submit"** (Отправить) в правом верхнем углу

2. **Вы получите два ключа:**

   **🔑 Site Key (Ключ сайта)**
   - Формат: `6Lc...AAAA...` (обычно начинается с `6Lc`)
   - Длина: около 40 символов
   - **Этот ключ МОЖНО показывать** - он публичный
   - Используется в JavaScript на фронтенде

   **🔐 Secret Key (Секретный ключ)**
   - Формат: `6Lc...AAAA...` (обычно начинается с `6Lc`)
   - Длина: около 40 символов
   - **Этот ключ НЕЛЬЗЯ показывать** - храните в секрете!
   - Используется только на сервере (в .env файле)

3. **Скопируйте оба ключа** и сохраните их в безопасное место

**💡 Как выглядят ключи:**
```
Site Key:    6LcXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
Secret Key:  6LcYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
```

---

### Шаг 4: Настройка параметров (Опционально)

После создания вы можете настроить дополнительные параметры:

#### Security preference (Настройки безопасности)
Вы увидите ползунок с настройками:
- **Low security** - пропускает больше запросов
- **Medium security** - рекомендуется ✅
- **High security** - строже проверяет, может блокировать VPN/прокси

**Рекомендация:** Оставьте **Medium** для начала

#### Additional settings
- **Send alerts to owners** - получать уведомления о подозрительной активности
- Можно оставить включенным ✅

---

### Шаг 5: Добавление ключей в проект

После получения ключей откройте файл `.env` в вашем проекте:

```bash
# В корне проекта /home/sernatalia/booking/.env
```

Найдите строки с reCAPTCHA и **раскомментируйте их** (уберите символ `#`):

**Было:**
```env
# Google reCAPTCHA v3 (ВРЕМЕННО ОТКЛЮЧЕНО ДЛЯ ТЕСТИРОВАНИЯ)
# RECAPTCHA_SITE_KEY=6LcYQyYsAAAAABKewQKhThW__4xeweVP40GCkpgg
# RECAPTCHA_SECRET_KEY=6LcYQyYsAAAAAGGZD2cYAc5Z8i_eOOMqmlhyznHb
```

**Стало:**
```env
# Google reCAPTCHA v3
RECAPTCHA_SITE_KEY=ВАШ_НОВЫЙ_SITE_KEY
RECAPTCHA_SECRET_KEY=ВАШ_НОВЫЙ_SECRET_KEY
```

**Пример с реальными ключами:**
```env
# Google reCAPTCHA v3
RECAPTCHA_SITE_KEY=6LcXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
RECAPTCHA_SECRET_KEY=6LcYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
```

---

### Шаг 6: Перезапуск сервера

После добавления ключей в `.env` необходимо **перезапустить сервер**:

**Если сервер запущен через `npm`:**
```bash
# Остановите сервер (Ctrl+C в терминале)
# Затем запустите снова:
npm run dev
```

**Если сервер запущен через PM2 (на production):**
```bash
pm2 restart booking-form
```

---

### Шаг 7: Проверка работы

1. **Откройте консоль браузера** (F12 → вкладка Console)

2. **Обновите страницу** с формой

3. **Проверьте логи в консоли:**

   Вы должны увидеть:
   ```
   reCAPTCHA script loaded
   reCAPTCHA готова
   ```

4. **Заполните форму** и нажмите "Отправить"

5. **Проверьте логи сервера:**
   ```
   ✅ reCAPTCHA protection: enabled
   Verifying reCAPTCHA token
   reCAPTCHA verification result
   ```

6. **Если все работает:**
   - Форма отправляется успешно
   - Приходит уведомление в Telegram
   - Данные сохраняются в Google Sheets
   - Показывается благодарственное сообщение

---

## 🔍 Где найти ваши ключи позже

Если вы потеряли ключи или хотите изменить настройки:

1. Перейдите в **reCAPTCHA Admin Console**:
   ```
   https://www.google.com/recaptcha/admin
   ```

2. Войдите в свой Google аккаунт

3. Вы увидите список всех ваших сайтов reCAPTCHA

4. Нажмите на нужный сайт (например, "Booking Form")

5. Вы увидите:
   - **Site Key** - виден сразу
   - **Secret Key** - нажмите "Show" чтобы увидеть

---

## ❓ Частые проблемы и решения

### Проблема 1: "Invalid site key"
**Причина:** Неправильно скопирован Site Key или забыли добавить домен

**Решение:**
- Проверьте, что Site Key скопирован полностью
- Убедитесь, что `localhost` добавлен в список доменов в настройках reCAPTCHA

### Проблема 2: "reCAPTCHA verification failed"
**Причина:** Неправильный Secret Key или проблемы с подключением к Google API

**Решение:**
- Проверьте, что Secret Key скопирован полностью
- Убедитесь, что сервер имеет доступ к интернету
- Проверьте логи сервера для деталей ошибки

### Проблема 3: Не видно reCAPTCHA badge (значка) внизу страницы
**Это нормально!** reCAPTCHA v3 работает невидимо. Если вы хотите увидеть badge:
- Он появляется внизу справа как маленький значок "reCAPTCHA"
- Это нормальное поведение для v3

### Проблема 4: "Missing required parameter: response"
**Причина:** Токен reCAPTCHA не отправляется на сервер

**Решение:**
- Проверьте, что скрипт reCAPTCHA загружается (смотрите консоль браузера)
- Проверьте, что Site Key правильный в `.env`
- Попробуйте очистить кеш браузера и перезагрузить страницу

---

## 🔒 Безопасность

### ✅ Что МОЖНО делать:
- Показывать Site Key в коде (он публичный)
- Добавлять Site Key в HTML/JavaScript
- Делиться Site Key с коллегами

### ❌ Что НЕЛЬЗЯ делать:
- Показывать Secret Key кому-либо
- Добавлять Secret Key в JavaScript (только на сервере!)
- Коммитить Secret Key в Git (используйте .env!)
- Публиковать Secret Key в интернете

---

## 📊 Мониторинг и статистика

После настройки вы можете отслеживать статистику:

1. Зайдите в [reCAPTCHA Admin Console](https://www.google.com/recaptcha/admin)
2. Выберите ваш сайт
3. Перейдите на вкладку **"Analytics"**

Там вы увидите:
- Количество запросов
- Распределение score (оценки пользователей)
- Заблокированные попытки
- Географию запросов

---

## 🚀 Production Deployment

Когда будете деплоить на production:

1. **Добавьте ваш production домен** в настройках reCAPTCHA:
   ```
   https://www.google.com/recaptcha/admin
   → Выберите ваш сайт
   → Settings
   → Domains
   → Добавьте: yourdomain.ru
   ```

2. **Обновите `.env` на сервере** с теми же ключами

3. **Перезапустите сервер** через PM2

4. **Протестируйте** с вашего production домена

---

## 📞 Если что-то не получается

Если у вас возникли проблемы:

1. **Проверьте логи браузера** (F12 → Console)
2. **Проверьте логи сервера** (`npm run dev` или `pm2 logs`)
3. **Убедитесь, что:**
   - Оба ключа скопированы правильно
   - Нет лишних пробелов в `.env`
   - Сервер перезапущен после изменения `.env`
   - `localhost` добавлен в домены reCAPTCHA

Если проблема не решается, напишите мне детали ошибки из логов!

---

**Удачи с настройкой! 🎉**
